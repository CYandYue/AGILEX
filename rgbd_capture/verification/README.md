# 验证记录

- 13 项离线测试通过：延迟位姿关联与解析轨迹对照、字节序/行填充、尺寸及像素模型校验、里程计缺口、时钟倒退、无效深度、RGB-D 错时、畸变导出、浮点深度和 NaN 保留、延后到达及变化的外参、损坏/不完整 bag 拒绝、原始序列化消息保存。
- `synthetic_live.json` 为私有 ROS master 中的在线测试结果：合成 640×480 RGB-D 输入、通过 `run.sh record` 录制、Ctrl-C 收尾、离线位姿复算验证。结果为 35 个完整帧组，约 6 Hz。
- 两个 shell 入口通过语法与帮助参数检查；传感器 launch 参数展开确认 640×480、30 Hz、深度对齐和驱动帧组同步开启。
- 本次未启动真实传感器或底盘；合成图像压缩率较高，文件体积不是实景录制带宽指标。仍需实测联合负载、运动同步和深度融合质量。

运行方法见上一级 README。最后一项源码调整仅抑制浮点无效深度比较的 NumPy 预期警告，随后重新通过全部 13 项离线测试；在线结果文件保留此前完整入口测试的原始输出。
